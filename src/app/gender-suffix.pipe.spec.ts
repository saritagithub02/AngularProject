import { GenderSuffixPipe } from './gender-suffix.pipe';

describe('GenderSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
