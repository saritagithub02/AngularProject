import { ImpuresortArrPipe } from './impuresort-arr.pipe';

describe('ImpuresortArrPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpuresortArrPipe();
    expect(pipe).toBeTruthy();
  });
});
