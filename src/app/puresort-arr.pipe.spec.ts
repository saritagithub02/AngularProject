import { PuresortArrPipe } from './puresort-arr.pipe';

describe('PuresortArrPipe', () => {
  it('create an instance', () => {
    const pipe = new PuresortArrPipe();
    expect(pipe).toBeTruthy();
  });
});
